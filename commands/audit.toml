prompt = """
Execute a comprehensive, read-only architectural audit of the project to establish deep context. Do not modify any files. Systematically map the codebase, explicitly excluding dependency directories (e.g., node_modules, vendor), build artifacts, and binaries. Focus on identifying core patterns, domain boundaries, and cross-module dependencies. Conclude by outputting a concise, high-level summary of the architecture to confirm your readiness.
"""
